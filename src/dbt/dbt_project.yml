name: fantasy_br
version: "1.0.0"

profile: fantasy_br

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

seeds:
  fantasy_br:
    player_overrides:
      +column_types:
        id: int64
        name: string
        position: string
        club: string

models:
  fantasy_br:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
      scouting:
        +materialized: ephemeral
      start_or_sit:
        +materialized: ephemeral
      market_valuation:
        +materialized: ephemeral
    scouting:
      +materialized: table
    start_or_sit:
      +materialized: table
    market_valuation:
      +materialized: table
    squad_and_team:
      +materialized: table
