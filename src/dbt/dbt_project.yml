name: fantasy_br
version: "1.0.0"

profile: fantasy_br

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  fantasy_br:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    scouting:
      +materialized: table
    start_or_sit:
      +materialized: table
    market_valuation:
      +materialized: table
